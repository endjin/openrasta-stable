namespace OpenRasta.Pipeline
{
    internal class PipelineDataConstants
    {
        internal const string PipelineState = OrPipeline + "PipelineStage";

        internal const string HandlerType = OrPipeline + "HandlerType";

        internal const string Operations = OrPipeline + "Operations";

        internal const string OrPipeline = "__OR_PIPELINE_";

        internal const string ResourceKey = OrPipeline + "ResourceKey";

        internal const string ResponseCodec = OrPipeline + "ResponseCodec";

        internal const string SelectedHandlers = OrPipeline + "SelectedHandlers";

        internal const string SelectedResource = OrPipeline + "SelectedResource";
    }
}